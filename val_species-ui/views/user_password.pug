extends layout

block content

  h3 Reset Confirmed: Create New Password

  form(method='POST' action='/user/password')
    div.form-group
      div(class='row')
        div(class='col-sm-4')
          table(class='table')
            tr
              td Password
              td
                input#password.form-control(type='text', name='password' value=user.password)
            tr
              td Confirm Password
              td
                input#confirm.form-control(type='text', name='confirm' value=user.confirm)

    button.btn.btn-primary(type='submit') Save
    a(href=`/`)
      button.btn(type='button') Cancel
